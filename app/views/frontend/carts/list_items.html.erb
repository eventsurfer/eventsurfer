<table class="table">
  <th>Performance</th>
  <th>PerformanceID</th>
  <tbody>
  <% if !@items.nil?%>
    <% @items.each do |ticket| %>
      <tr>
        <td><%= ticket.performance.event.name%></td>
        <td><%= ticket.performance.id%></td>
        <td><a href=<%= frontend_remove_from_cart_path(ticket.id)%>>X</a></td>
      </tr>
    <% end %>
  <%= @cart %>
  <% end %>
  </tbody>
</table>