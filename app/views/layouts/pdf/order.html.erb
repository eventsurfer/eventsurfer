<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Title</title>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
  <style>
    p {
      margin-bottom: 0
    }
  </style>
</head>
<body>
<br><br>
<div class="container">
  <label>
    <small><%= @info.company %> <%= @info.street %> <%= @info.street_number %>
      | <%= @info.postcode %> <%= @info.city %></small>
  </label>
  <div class="row shorter">
    <div class="col-sm-4">
      <p class="destination"><%= @user.name %></p>
      <p class="destination"><%= "Destination street and number" %></p>
      <p class="destination"><%= "Postcode and city" %></p>
    </div>
    <div class="col-sm-4 "></div>
    <div class="col-sm-4">
      <p class=" source"><%= @info.company %></p>
      <p class=" source"><%= @info.street %> <%= @info.street_number %></p>
      <p class=" source"><%= @info.postcode %> <%= @info.city %></p>
      <p class=" source"><%= @info.cellphone %></p>
      <p class=" source"><%= @info.email %></p>
      <p class=" source"><%= @info.website %></p>
    </div>
  </div>
  <br><br>
  <div id="content">
    <h3><%= "Order" %></h3>
    <div id="general">
      <div class="row">

        <div class="col-sm-4">
          <p><%= "Order Nr.:" + @order.id.to_s %></p>
          <p><%= "Commission: " %><%= "Ticket Order" %></p>
          <p><%= "User number: " %><%= @order.user_id %></p>
          <p></p>
        </div>
        <div class="col-sm-4"></div>
        <div class="col-sm-4">
          <p><%= "Date of order: " + I18n.l(@order.created_at.to_date, format: :shortest).to_s%></p>
          <p><%= "Delivery date: " + I18n.l(@order.created_at.to_date, format: :shortest).to_s%></p>
          <p><%= "Processor: " %><%= "Autogenerated" %></p>
          <p><%= "Phone: " %><%= @info.cellphone %></p>
          <p><%= "Mail: " %><%= @info.email %></p>
        </div>
      </div>
    </div>
    <br><br><br><br>
    <% count = 1 %>
    <div id="table text-center">
      <table id="order" class="table">
        <thead>
        <tr>
          <th>Pos</th>
          <th>Description</th>
          <th>Number of Tickets</th>
          <th>Unit Price €</th>
          <th>Subtotal</th>
        </tr>
        </thead>
        <tbody>
        <% total = [] %>
        <% @groupTickets.each do |groupTicket| %>
          <tr>
            <td><%= count %></td>
            <td><%= @performances.find(groupTicket.performance_id).event.name %>, <%= @performances.find(groupTicket.performance_id).location.name %> (<%= I18n.l(@performances.find(groupTicket.performance_id).start) %>)</td>
            <td><%= groupTicket.count %></td>
            <td><%= groupTicket.single_price %></td>
            <td><%= total.push( (groupTicket.single_price * groupTicket.count.to_f).round(2)).last %></td>
          </tr>
          <% count += 1 %>
        <% end %>
        </tbody>
      </table>
    </div>
    <div id="total">
      <div class="row">
        <div class="col-sm-8"></div>
        <div class="col-sm-4">
          <p>Total: <%= total.inject(0){|sum,x| sum + x } %> €</p>
        </div>
      </div>
    </div>
  </div>
</div>
</body>
</html>